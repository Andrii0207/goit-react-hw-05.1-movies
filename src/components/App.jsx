import { getTrendingMovies } from '../service/moviesAPI';

export const App = () => {
  getTrendingMovies();
  return (
    <div
      style={{
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: 40,
        color: '#010101',
      }}
    >
      React homework template
    </div>
  );
};
